{
  "schemaVersion": 1,
  "obligations": [
    {
      "id": "run-the-gate-that-covers-this-change",
      "title": "Run the gate that covers this change",
      "question": "Each site names its coveringGate — the gate over the artifact this change invalidates. You do not choose it and must not answer about a different one (ADR 0098). Determine wiredBy for that gate by reading, not running — grep the .husky hooks, the .github/workflows jobs, and the yarn gates:run manifest (tools/scripts/run-assertion-gates.mjs) at the head commit for the gate's script name, and write the one that invokes it, or \"none\" if nothing does (ADR 0074). command and exitCode are optional: record them only when you also ran the checker's own script directly through node, never through yarn or corepack yarn; when you did not run it, write \"not run\" in both. Cite wiredBy: the file, the line number, and the exact text of the line that invokes the gate; a wiredBy of \"none\" carries no citation, because nowhere has no line.",
      "answerFields": ["command", "exitCode", "wiredBy"],
      "siteFields": ["coveringGate"],
      "citedFields": [
        {
          "field": "wiredBy",
          "uncitedWhen": "none"
        }
      ],
      "defect": "wiredBy \"none\" is a finding: per ADR 0074 the checker is not a gate, so nothing would have caught this on main. A non-zero exitCode, when one was obtained, is also a finding, with the command and exit code as executed evidence — raised the ordinary way (an obligation answer that exposes a defect is written up like any other finding), not mechanically by defectWhen, because exitCode is optional and its \"not run\" value cannot be told apart from a real code by a plain equality check.",
      "trigger": {
        "paths": [
          {
            "glob": "package.json",
            "addedPattern": "\"version\"\\s*:",
            "coveringGate": "openapi:check"
          },
          {
            "glob": "libs/app-api-client/**",
            "coveringGate": "openapi:check"
          },
          {
            "glob": "libs/api-specs/**",
            "coveringGate": "openapi:check"
          },
          {
            "glob": "apps/backend/src/swagger/**",
            "coveringGate": "openapi:check"
          },
          {
            "glob": "apps/backend/src/prisma/migrations/**",
            "coveringGate": "prisma:migrations:check"
          },
          {
            "glob": "libs/design-tokens/src/tokens.json",
            "coveringGate": "design-tokens:check"
          }
        ]
      },
      "seededFrom": "#408",
      "goldenCase": "release-bump-leaves-generated-client-stale",
      "defectWhen": {
        "field": "wiredBy",
        "equals": "none"
      }
    },
    {
      "id": "destructive-confirmation-names-what-it-mutates",
      "title": "A destructive confirmation names everything it destroys",
      "question": "For each site: follow the confirmed code path and list every persisted key, store, or record it writes or clears; quote the confirmation text verbatim; then state whether the text names everything in that list. Do not infer the mutation set from the handler's name. Cite confirmationText: the file, the line number, and the exact text of the line the message starts on.",
      "answerFields": ["mutates", "confirmationText", "namesEverything"],
      "citedFields": ["confirmationText"],
      "defect": "namesEverything false is a finding. A whole-vault or whole-account destructive action behind a bare window.confirm rather than a dialog is also a finding.",
      "trigger": {
        "paths": ["apps/**", "libs/**"],
        "addedPattern": "window\\.confirm\\(|AlertDialog|Are you sure"
      },
      "seededFrom": "#657",
      "goldenCase": "import-confirm-is-bare-window-confirm",
      "defectWhen": {
        "field": "namesEverything",
        "equals": false
      }
    },
    {
      "id": "slot-injected-props-land-on-the-control",
      "title": "Injected props land on the control, not on a wrapper",
      "question": "For each site: name the element that is the direct child of the Slot-based component, state where the injected id, aria-describedby and aria-invalid land, and state whether that element is the focusable control the user types into. Cite slotChild: the file, the line number, and the exact text of the direct child's own line — that line, not the Slot-based component's.",
      "answerFields": ["slotChild", "propsLandOn", "isFocusableControl"],
      "citedFields": ["slotChild"],
      "defect": "isFocusableControl false is a finding: Radix Slot forwards its props to its single direct child, so a wrapper element between FormControl and the input takes the label association and the input is announced by its placeholder or not at all.",
      "trigger": {
        "paths": ["apps/**", "libs/**"],
        "addedPattern": "<FormControl|Slot\\b"
      },
      "seededFrom": "#525",
      "goldenCase": "signup-password-wrapper-inside-formcontrol",
      "defectWhen": {
        "field": "isFocusableControl",
        "equals": false
      }
    },
    {
      "id": "env-assignment-runtime-value",
      "title": "An environment assignment stores what it looks like it stores",
      "question": "For each site: quote the right-hand side of the assignment as written, state the value actually stored after the process.env setter coerces it, and state whether that is what the surrounding code reads as intending. Cite assignedExpression: the file, the line number, and the exact text of the assignment line.",
      "answerFields": ["assignedExpression", "runtimeValue", "matchesIntent"],
      "citedFields": ["assignedExpression"],
      "defect": "matchesIntent false is a finding. Assigning undefined or null to unset a variable is the common shape: the setter coerces to string, so the key holds the truthy literal \"undefined\". delete is what unsets it.",
      "trigger": {
        "paths": ["apps/**", "libs/**", "tools/**"],
        "addedPattern": "process\\.env\\.[A-Za-z_][A-Za-z0-9_]*\\s*=(?!=)"
      },
      "seededFrom": "#409",
      "goldenCase": "mail-test-setup-assigns-undefined-to-env",
      "defectWhen": {
        "field": "matchesIntent",
        "equals": false
      }
    },
    {
      "id": "enum-fanout-reaches-a-pinned-table",
      "title": "A fan-out over a guarded enum reaches a pinned table",
      "question": "Each site names its guardedEnum — a domain enum where handling some members and not others loses data with no error — and its coveringGate. Read the enum's members at the head commit. The site is a line the diff added; on the enum's own file it may belong to a neighbouring enum, and the question is about guardedEnum all the same. Find the consumers at the head that fan out over the members: a scope that names two or more of them, as guardedEnum.Member references, as the member values used as property names or string literals, in an if-chain, or as the keys of an object literal. List them in consumers, each marked pinned or hand-enumerated, with the members a hand-enumeration leaves out. Then pick the one consumer the rest of the answer is about: when the site is in a consumer, the fan-out that contains the site; when the site adds a member to the enum, a hand-enumerating consumer that does not handle the new member — it is usually in a file the diff never touched — or, if every consumer is pinned, the pinned table. Cite enumeration: the file, the line number, and the exact text of the first line of that consumer's enumeration. pinned is true only if that consumer reaches one table typed `satisfies Record<guardedEnum, …>`, which fails to compile when a member is missing; a hand-enumeration is not pinned even when it happens to list every member today, because it compiles just as well when the next one is added. omits lists the members that consumer leaves out, or \"none\". Determine wiredBy for coveringGate by reading the .husky hooks, the .github/workflows jobs, and the yarn gates:run manifest at the head commit for the gate's script name, and write the line that invokes it, or \"none\" if nothing does or the gate does not exist at this head.",
      "answerFields": [
        "consumers",
        "enumeration",
        "pinned",
        "omits",
        "wiredBy"
      ],
      "siteFields": ["guardedEnum", "coveringGate"],
      "citedFields": [
        "enumeration",
        {
          "field": "wiredBy",
          "uncitedWhen": "none"
        }
      ],
      "defect": "pinned false with wiredBy \"none\" is a finding, located at that consumer: a hand-enumerated fan-out compiles while handling some members and not others, and nothing fails when the enum grows, so a member it does not list is silently skipped, dropped, or destroyed — #512 lost grocery ciphertext when Groceries was added under a five-member reconcile, and #537 found the hardened export dropping the Tasks blob it was never written to list. A non-empty omits makes it worse, not different. When coveringGate is wired, the gate fails the scopes it covers and the finding is suppressed there (ADR 0074); a consumer outside its scope is still a finding, raised the ordinary way.",
      "trigger": {
        "onePerFile": true,
        "excludePaths": [
          "**/*.test.ts",
          "**/*.test.tsx",
          "**/*.spec.ts",
          "**/*.spec.tsx"
        ],
        "paths": [
          {
            "glob": "libs/app-api-client/src/api.ts",
            "addedPattern": "^\\s*[A-Z][A-Za-z0-9]*:\\s*'[A-Za-z0-9]+',?\\s*$",
            "guardedEnum": "VaultBlobType",
            "coveringGate": "enum:fanout:check"
          },
          {
            "glob": "libs/web-vault/src/**",
            "addedPattern": "\\bVaultBlobType\\.[A-Z]",
            "guardedEnum": "VaultBlobType",
            "coveringGate": "enum:fanout:check"
          },
          {
            "glob": "libs/vault-core/src/**",
            "addedPattern": "\\bVaultBlobType\\.[A-Z]",
            "guardedEnum": "VaultBlobType",
            "coveringGate": "enum:fanout:check"
          }
        ]
      },
      "seededFrom": "#512",
      "goldenCase": "groceries-blob-type-without-fanouts",
      "defectWhen": {
        "all": [
          {
            "field": "pinned",
            "equals": false
          },
          {
            "field": "wiredBy",
            "equals": "none"
          }
        ]
      }
    }
  ]
}
